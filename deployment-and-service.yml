apiVersion: apps/v1
kind: Deployment
metadata:
  name: swiggy-clone-app
  labels:
    app: swiggy-clone-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: swiggy-clone-app
  template:
    metadata:
      labels:
        app: swiggy-clone-app
    spec:
      terminationGracePeriodSeconds: 30
      containers:
      - name: swiggy-clone-app
        image: ursulan1/swiggy
        imagePullPolicy: "Always"
        ports:
        - containerPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: swiggy-clone-app
  labels:
    app: swiggy-clone-app
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 3000
  selector:
    app: swiggy-clone-app
